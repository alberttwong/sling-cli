import 'package:flutter/material.dart';

class TaskNewSourceFilesPage extends StatelessWidget {
  static const routeName = '/task/new/source-files';
  const TaskNewSourceFilesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
