import 'package:flutter/material.dart';

class TaskNewTargetConfigPage extends StatelessWidget {
  static const routeName = '/task/new/target-config';
  const TaskNewTargetConfigPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
