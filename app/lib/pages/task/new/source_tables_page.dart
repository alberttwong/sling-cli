import 'package:flutter/material.dart';

class TaskNewSourceTablesPage extends StatelessWidget {
  static const routeName = '/task/new/source-tables';
  const TaskNewSourceTablesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
