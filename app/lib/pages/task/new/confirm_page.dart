import 'package:flutter/material.dart';

class TaskConfirmPage extends StatelessWidget {
  static const routeName = '/task/new/confirm';
  const TaskConfirmPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
