import 'package:flutter/material.dart';

class TaskHistoryPage extends StatelessWidget {
  static const routeName = 'task/history';
  const TaskHistoryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
