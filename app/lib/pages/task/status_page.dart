import 'package:flutter/material.dart';

class TaskStatusPage extends StatelessWidget {
  static const routeName = 'task/status';
  const TaskStatusPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
